apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-ui
  namespace: saga-go
  labels:
    app.kubernetes.io/component: kafka-ui
    app.kubernetes.io/part-of: saga-go
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka-ui
  template:
    metadata:
      labels:
        app: kafka-ui
    spec:
      containers:
        - name: kafka-ui
          image: ghcr.io/kafbat/kafka-ui:v1.5.0
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: KAFKA_CLUSTERS_0_NAME
              value: local
            - name: KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS
              value: kafka-kafka-bootstrap:9092
---
apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: saga-go
  labels:
    app.kubernetes.io/component: kafka-ui
    app.kubernetes.io/part-of: saga-go
spec:
  selector:
    app: kafka-ui
  ports:
    - name: http
      port: 8080
      targetPort: http